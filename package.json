{
  "name": "lanscaping",
  "version": "1.0.0",
  "description": "An uptdated version of a website i created in a coding bootcamp",
  "main": "index.html",
  "scripts": {
    "lite": "live-server",
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  },
  "dependencies": {
    "bootstrap": "^4.5.2",
    "jquery": "^3.5.1",
    "popper.js": "^1.16.1"
  }
}
